catmaidCheck <- function(dateSince, user = 'all'){#date format as YYY-MM-DD string or a date value
  #if (!is.date(dateSince)){
    since = as.Date(dateSince)
  #}
  #else{
  #  since = dateSince
  #}
  
  
}